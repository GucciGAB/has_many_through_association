<%= form_with(model: @physician, class: 'max-w-lg mx-auto bg-white p-8 rounded-lg shadow-lg') do |form| %>
  <h2 class="text-3xl font-semibold text-gray-800 mb-6">Physician Information</h2>

  <div class="mb-4">
    <%= form.label :name, class: 'block text-lg font-medium text-gray-700' %>
    <%= form.text_field :name, class: 'mt-2 p-3 w-full border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500' %>
  </div>

  <div class="mb-6">
    <%= form.submit 'Save Physician', class: 'w-full py-3 px-4 bg-green-600 text-white font-medium rounded-lg hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-green-500 transition duration-200' %>
  </div>
<% end %>
